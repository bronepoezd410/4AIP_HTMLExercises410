"use strict";

   // let arr = ['a', 'b', 'c', 'd', 'e'];

   // for (let k = 0; k <= arr.length - 1; k++) {
   //    console.log(arr[k]);
   // };


   // let arr = ['a', 'b', 'c', 'd', 'e'];

   // for (let i = 0; i <= arr.length - 1; i++) {
   //    if (i == 0 || i == arr.length - 1) {
   //       continue;
   //    }
   //    console.log(arr[i]);
   // };


   // let arr = ['a', 'b', 'c', 'd', 'e'];
   // let i = arr.length - 1;

   // for (i; i >= 0; i--) {
   //    console.log(arr3[i])
   // };


   let arr = ['a', 'b', 'c', 'd', 'e'];

   for (let i = 0; i <= arr.length - 1; i++) {
      console.log(arr[i]);
   }